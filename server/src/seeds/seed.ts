// Seed script will be implemented in Phase 7
import db from '../config/connection.js';

db.once('open', async () => {
  console.log('Seed script placeholder — will be implemented in Phase 7');
  process.exit(0);
});
